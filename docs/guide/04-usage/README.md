# 04 · Usage

← [Guide](../README.md)

The practical manual — install, first run, every flag.

| Page | Covers |
|---|---|
| [CLI reference](cli-reference.md) | Install methods, the `pgfathom setup` guide, schema-scope control, full flag reference, the three output formats |

**Deliberately a thin section.** Room to grow — a guided walkthrough ("first run against a
real database, from zero") or troubleshooting notes belong here as new pages rather than
bloating [CLI reference](cli-reference.md).

## See also

- [01 · Overview](../01-overview/README.md) — the verdicts `discover` output produces,
  before diving into flags
- [03 · Features & Safety](../03-features-and-safety/README.md) — why the default scope is
  `public` and nothing else
