# binários e artefatos de build
/bin/
/dist/
/pgfathom
*.exe

# cobertura e perfis
*.out
*.test
/coverage/

# configuração de agente de código (regenerável: openspec init --tools claude)
.claude/

# artefatos que a ferramenta gera ao rodar contra um banco: são nomes de objeto
# de um schema de terceiro, e o `setup` sugere este diretório por padrão
/pgfathom-out/

# autocompletar gerado do próprio comando durante o build
/completions/

# corpus de benchmark: a receita é versionada em bench/corpus.toml, os dumps não
/bench/cache/
/bench/local/

# editor e sistema
.DS_Store
.idea/
.vscode/
